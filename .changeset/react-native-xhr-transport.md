---
"@flamefrontend/sse-runtime-react-native": minor
---

Add a React Native XMLHttpRequest transport for streaming SSE responses in runtimes where fetch does not expose a compatible response body stream.
